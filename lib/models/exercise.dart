class Exercise {
  String? name;
  String? imageName;
  String? imageUrl;

  Exercise(reName, reImageName, reImageUrl) {
    name = reName;
    imageName = reImageName;
    imageUrl = reImageUrl;
  }
  //const Exercise({
  // @required this.name,
  //this.imageUrl,
  //}) : assert(name != null);

  // final String name;
  //final String imageUrl;
}
