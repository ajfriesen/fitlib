class Exercise {
  String name;
  String imageUrl;

  Exercise(reName, reImageUrl) {
    name = reName;
    imageUrl = reImageUrl;
  }

  //const Exercise({
  // @required this.name,
  //this.imageUrl,
  //}) : assert(name != null);

  // final String name;
  //final String imageUrl;
}
