class Exercise {
  String name;
  String imageUrl;

  Exercise(re_name, re_imageUrl) {
    name = re_name;
    imageUrl = re_imageUrl;
  }

  //const Exercise({
  // @required this.name,
  //this.imageUrl,
  //}) : assert(name != null);

  // final String name;
  //final String imageUrl;
}
