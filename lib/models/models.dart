class Exercise {
  String? name;
  String? imageName;

  Exercise(reName, reImageName) {
    name = reName;
    imageName = reImageName;
  }

  //const Exercise({
  // @required this.name,
  //this.imageUrl,
  //}) : assert(name != null);

  // final String name;
  //final String imageUrl;
}
